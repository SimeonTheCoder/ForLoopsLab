using System;

namespace SoftUni
{
 	class CharSequence
    {
    	static void Main(string[] args)
        {
         	string s = Console.ReadLine();
          
          	for(int i = 0; i < s.Length; i++)
            {
             	Console.WriteLine(s[i]); 
            }
        }
    }
}